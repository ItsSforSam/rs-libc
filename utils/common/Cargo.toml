[package]
name = "rslibc-common"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rslibc-syscall = {path = "../syscall"}
log = {workspace = true}
[features]
threads = []