[package]
name = "crt0"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
# cfg-if = "1.0.4"
compiler_builtins = {version = "0.1.160", features = ["mem","mangled-names"]}

[lib]
crate-type = ["staticlib"]

[lints]
workspace = true
