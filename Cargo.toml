[package]
name = "rs-libc"
version.workspace = true
edition.workspace = true
publish = true
[dependencies]
api           = {path = "api"}

[workspace]
members = ["crt0", "api", "api-sys", "utils", "build-utils","arch"]

[build-dependencies]
cc = "1.2.46"
[workspace.package]
version = "0.0.0"
edition = "2024"
publish = false
[features]


[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
