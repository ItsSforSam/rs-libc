[package]
name = "arch-meta"
edition.workspace = true
description = "Simply a meta package to deal with compiling asm"


# [lints]
# workspace = true
[lib]
path = ".meta.rs"

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }